# 实时解调性能优化记录

## 问题诊断

实时解调（RTL-SDR/FM）出现音频卡顿，处理 26s 实际音频仅产出 22s，说明存在丢帧。

**根因**：单帧处理耗时 > 单帧信号时长，`audioDeviceWriter` 缓冲区欠载产生音频空洞。

**放大因素**：`state.audio_accum = [state.audio_accum; audio_frame]` 逐帧拼接为 O(n²) 操作——第 k 帧时复制 ~k×N_audio 个样点，运行越久越慢。

---

## 已实施优化

### 1. 频道选择 FIR 滤波器缩短（80 dB → 60 dB）

| 项目 | 优化前 | 优化后 |
|------|--------|--------|
| 阻带衰减 | 80 dB | 60 dB |
| FIR 抽头数（FM @ 2.4MSPS）| ~670 | ~300 |
| 滤波运算量 | 100% | ~45% |
| 受影响位置 | — | `design_channel_filter()` ×2，`multistage_decimate_stream()` ×N |

**代价**：邻道抑制从 10000:1 → 1000:1，但仍比 RTL-SDR 8-bit ADC 量化噪声 (–48 dB) 低 12 dB，不可闻。

### 2. DDC 混频表预计算

| 项目 | 优化前 | 优化后 |
|------|--------|--------|
| exp() 计算 | 32768 次/帧 | 0（仅在 f_offset 变化时计算 1 次）|
| 额外内存 | 0 | 256 KB–1 MB |

**原理**：`exp(-1j·2π·f_offset·t)` 仅依赖 f_offset 和帧大小，与 IQ 数据无关。在处理循环中缓存在 `state.ddc_table`，`channel_select_stream()` 直接使用预计算表。

### 3. RTL-SDR 大帧支持（多块拼接）

| 项目 | 优化前 | 优化后 |
|------|--------|--------|
| 帧大小上限 | 32768（硬截断） | 131072（GUI 可选，多读拼接）|
| 帧数/分钟 | 4380 | 1095（@ 131072） |
| audio_accum O(n²) | 严重 | 大幅缓解 |

**实现**：`read_rtlsdr_frame()` 连续读取 `ceil(frame_size/32768)` 次硬件块，拼接为一个完整 IQ 帧。

### 4. 默认参数调整

| 参数 | 优化前 | 优化后 | 原因 |
|------|--------|--------|------|
| 中心频率 | 100 MHz | 96.5 MHz | 该频率有广播可直接验证 |
| RF 增益 | 20 dB | 40 dB | 信噪比更好 |

---

## 累计效果

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| FIR 抽头数 | ~670 | ~300（↓55%） |
| DDC exp/帧 | 32768 次 | 0 次 |
| RTL-SDR 最大帧 | 32768 | 131072 |
| 单帧处理耗时 | > 帧时长（卡顿） | 实际需测试 |

目标：单帧处理时间 < 帧时长的 50%，从根源消除音频欠载。
